# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/pcvform

# Admin credentials (seeded on first startup if not already in DB)
ADMIN_EMAIL=admin@getpayedmail.com
ADMIN_PASSWORD=admin123

# Server port
PORT=3001

# SMTP (optional — for voucher email sending)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
